function quicksort(array)
{
    var low = 0;
    var high = array.length;
    // Low equals or exceeds high, we're done, otherwise...
    if(low < high)
    {
        // Partition and swap
    }
    return array;
}

function partition(array, low, high)
{

}

function swap(array, low, high)
{
    var temp = array[low];
    array[low] = array[high];
    array[high] = temp;
}